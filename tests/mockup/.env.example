# Copy this file to tests/mockup/.env and adjust values if needed.
# DO NOT commit the actual tests/mockup/.env file.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=appdb
POSTGRES_PORT=15432

HASURA_PORT=18080
HASURA_GRAPHQL_ADMIN_SECRET=testsecret
HASURA_GRAPHQL_ENABLE_CONSOLE=false
HASURA_GRAPHQL_DEV_MODE=true
HASURA_GRAPHQL_ENABLED_LOG_TYPES=startup,http-log,webhook-log,websocket-log,query-log
# Optional override if you need a custom value:
# This file is intended for local/mockup testing only.
# For staging/production, use a separate env file with non-default credentials.
# HASURA_GRAPHQL_DATABASE_URL=postgres://postgres:postgres@postgres:5432/appdb
